<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frazatas.utmlib2" >
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"     android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".LoginActivity"
            android:label="Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookBorrow"
            android:label="Borrow A Book">
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".HoldResult"></activity>
        <activity android:name=".HistoryFile"
            android:label="Borrowing History"></activity>
        <activity android:name=".RenewNews"
            android:label="BorrowingRenewal"></activity>
        <activity android:name=".SearchSearch"
            android:label="@null">
         </activity>
        <activity android:name=".BookMore"
            android:label="Borrowing History"></activity>
        <activity android:name=".UserInfo"
            android:label="User History"></activity>
        <activity android:name=".MainBarcode"
            android:label="User History"></activity>

    </application>
</manifest>
